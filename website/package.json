{
  "name": "hall-e-download-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "check": "node --check public/app.js && node --check public/mechanical-assembly.js && node scripts/build.mjs",
    "deploy": "npm run check && npx --yes wrangler@4.130.0 pages deploy dist --project-name hall-e --branch main"
  },
  "engines": { "node": ">=22" }
}
